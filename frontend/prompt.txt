throughout the codebase check for repeating code. clean up the codebase by creating reusable components based on the code we have so far. so for example extract all the button types. and use them. make sure to use my own colors. dont change any functionality.

make it so that logged in users are redirected to /my-matches when they visit / (the landing page)