[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --cov=backend --cov-report=term-missing --cov-fail-under=80
markers =
    unit: fast, isolated tests without app/db boot
    integration: endpoint and persistence tests using the ASGI app
